services:
  db:
    image: postgres:15-alpine
    environment:
       POSTGRES_PASSWORD: pass
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 5s
      retries: 5

  api: 
    build: ./api
    depends_on:
      db:
        condition: service_healthy